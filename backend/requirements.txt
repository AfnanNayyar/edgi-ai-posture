flask==3.0.3
flask-cors==4.0.1

# JWT auth for your existing /auth endpoints.
PyJWT==2.10.1

# Gunicorn is used by Render to run `gunicorn app:app`.
gunicorn==21.2.0

# Mediapipe and NumPy need versions that have wheels for Linux + Python 3.10.
numpy==1.26.4
mediapipe==0.10.11

# Headless OpenCV build (no GUI libs) for server environments like Render.
opencv-python-headless==4.8.1.78

# Mediapipe depends on protobuf; this range matches mediapipe 0.10.11.
protobuf>=3.20.2,<4
